function Phiq=PhiqEval(q)
% Evaluation of Phiq(q)=partial derevative matrix of Phi with respect to q
% This is where the Jacobian must be defined. Each term must be calculated outide of matlab.

Phiq=Phiq(q);

end
